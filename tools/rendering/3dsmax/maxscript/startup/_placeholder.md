# startup

Scripts that auto-load when Max starts. The repo path is added to Max's
startup script search list so these run on launch (one-line user-side
hook; document per-user when the first startup script lands).
