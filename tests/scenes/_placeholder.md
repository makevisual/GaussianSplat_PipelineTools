# scenes

DCC-native test scene files (`.max`, `.blend`, `.hip`, `.nk`) for
manual verification or for integration tests to open. Organised by
scenario, then version:

```
scenes/<scenario>/v001/<scene_file>
```
